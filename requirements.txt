# LangChain core
langchain>=0.3.0
langchain-community>=0.3.0
langchain-chroma>=0.2.0
langchain-huggingface>=0.1.0
langchain-classic>=0.1.0

# Document loaders
langchain-docling>=0.1.0
pandas>=2.0.0
# Vector store
chromadb>=0.5.0

# Embeddings & reranking
sentence-transformers>=3.0.0

# BM25 keyword search
rank-bm25>=0.2.2

# CLI output
rich>=13.0.0

# Graph DB (NetworkX for local implementation)
networkx>=3.0

# Orchestrator & LLM
langchain-openai>=0.2.0
langchain-google-genai>=2.0.0
langgraph>=0.2.0
python-dotenv>=1.0.0

# LangSmith Observability & Tracing
langsmith>=0.1.0

# Agent Memory (Mem0 — in-memory per session, no persistence needed)
mem0ai>=0.1.0
qdrant-client>=1.9.0   # used by Mem0 for in-memory vector store

# Web Backend (FastAPI)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
loguru>=0.7.0
redis>=5.0.0

# Web Frontend (Streamlit)
streamlit>=1.40.0
httpx>=0.27.0
graphviz>=0.20.0
